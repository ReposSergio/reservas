# Proyecto Prueba Reservas Hoteleras

## Apis

## POST /api/bookings -> Crear una nueva reserva
## GET /api/bookings/{id} -> Obtener una reserva por su ID
## PUT /api/bookings/{id} -> Actualizar una reserva existente
## DELETE /api/bookings/{id} -> Eliminar una reserva

# Dockerización de la Aplicación

Este proyecto incluye un backend en .NET, un frontend en Next.js y una base de datos PostgreSQL. La aplicación está completamente dockerizada para facilitar su ejecución.

## Requisitos
- Docker instalado (versión 20.10+)
- Docker Compose instalado (versión 2.0+)

## Pasos para ejecutar la aplicación
1. Clona este repositorio:
   ```bash
   git clone https://github.com/tu-repo/tu-proyecto.git
   cd tu-proyecto

